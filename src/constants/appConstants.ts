export enum Path {
  root = 'user',
  login = 'login',
  buildings = 'buildings',
  rooms = 'rooms',
  tenants = 'tenants',
  payments = 'payments',
  houseId = ':houseId',
  roomId = ':roomId',
  userId = ':userId',
}

export enum AppConst {
  token = 'token',
  userid = 'UserId',
}

export enum QueriesKey {}
