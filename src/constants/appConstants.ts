export enum Path {
  root = 'user',
  login = 'login',
  chat002 = 'chat002',
  chat003 = 'chat003',
  chat004 = 'chat004',
  userId = ':userId',
}

export enum AppConst {
  token = 'token',
  userid = 'UserId',
}

export enum QueriesKey {}
